import react from 'react';
import {View, Text} from 'react-native';

const Practice = ({label}) => {
  return (
    <View>
      <Text> beaucoup de texte {label}</Text>
    </View>
  );
};

export default Practice;
